# Google OAuth Client ID (required — create at console.cloud.google.com)
VITE_GOOGLE_CLIENT_ID=

# Optional: pre-fill Sheet ID (otherwise user enters it on first login)
VITE_SHEET_ID=

# Optional: restrict access to specific emails (comma-separated; leave blank for all Google accounts)
VITE_ALLOWED_EMAILS=
