{
  "slug": "custom-document-store-boolean",
  "frontend_id": "10047",
  "title": "Document Store with Boolean Predicate Search",
  "difficulty": "Hard",
  "topics": ["Design", "Stack", "String", "Hash Table"],
  "companies": ["Snowflake"],
  "tags": [
    "_prob::design::document-store",
    "_prob::expression::boolean-predicate-eval",
    "_ds::hash-table::inverted-index",
    "_ds::stack::two-stack-eval",
    "pat_topic::stack-topic::expression-eval",
    "pat_ds::string::stack-topic::expression-eval",
    "pat_group::expression"
  ],
  "leetcode_url": "",
  "description_html": "<div>\n  <p>Design an in-memory document service that stores documents and queries them with <strong>boolean predicate expressions</strong> over tokens.</p>\n  <h3>APIs</h3>\n  <ul>\n    <li><code>InsertDoc(filename, content)</code> — store a document. <code>filename</code> is a unique id; <code>content</code> is treated as a set of tokens.</li>\n    <li><code>CheckContains(filename, predicate) -&gt; bool</code> — does the named document satisfy the predicate?</li>\n    <li><code>GetAllFiles(predicate) -&gt; List[str]</code> — all filenames that satisfy the predicate.</li>\n  </ul>\n  <h3>Predicate semantics</h3>\n  <p>A bare token evaluates to whether the document contains it. Operators: <code>&amp;&amp;</code> (AND), <code>||</code> (OR), and <code>()</code> parentheses. Precedence: parentheses highest, then <code>&amp;&amp;</code> binds tighter than <code>||</code>. So <code>a &amp;&amp; b || c</code> means <code>(a &amp;&amp; b) || c</code>.</p>\n  <h3>Examples</h3>\n  <pre>Insert(f1, \"a b\"); CheckContains(f1, \"a\")              =&gt; true\nInsert(f1, \"a b\"); CheckContains(f1, \"c\")              =&gt; false\nInsert(f2, \"c\");   CheckContains(f2, \"a &amp;&amp; b || c\")     =&gt; true\nInsert(f3, \"a\");   CheckContains(f3, \"a &amp;&amp; (b || c)\")   =&gt; false\nInsert(f1,\"a b\"),(f2,\"c\"),(f3,\"a\"); GetAllFiles(\"a &amp;&amp; b || c\") =&gt; [\"f1\",\"f2\"]</pre>\n  <h3>Scale</h3>\n  <p>Up to 1e5 documents, 1e3 tokens/doc, 1e3 tokens/predicate.</p>\n  <h3>Approach</h3>\n  <ol>\n    <li><strong>Store</strong>: <code>Map&lt;filename, Set&lt;token&gt;&gt;</code> for O(1) membership, plus an <strong>inverted index</strong> <code>Map&lt;token, Set&lt;filename&gt;&gt;</code> for <code>GetAllFiles</code>.</li>\n    <li><strong>Evaluate the predicate</strong> with the classic operator-precedence parser — two stacks (operands + operators) or recursive descent, parens highest then <code>&amp;&amp;</code> over <code>||</code>. Operands resolve to booleans (token-membership) instead of numbers.</li>\n    <li><strong>CheckContains</strong>: evaluate against one doc's token set.</li>\n    <li><strong>GetAllFiles</strong>: evaluate the parse tree over the inverted index with <strong>set operations</strong> — <code>&amp;&amp;</code> = intersection, <code>||</code> = union — instead of re-checking every document.</li>\n  </ol>\n  <p>This is <strong>Basic Calculator III</strong> precedence/parens evaluation (LC 224 / 227 / 772) with boolean operands, layered on an <strong>inverted-index boolean-retrieval</strong> store. Closest LC: <strong>LC 772</strong> (precedence + parens eval), <strong>LC 1106</strong> (Parsing A Boolean Expression — boolean operators + parens).</p>\n</div>"
}
